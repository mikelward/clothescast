<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- TV: opt the app into the leanback launcher so it appears on the TV home screen.
         required="false" keeps the phone/tablet Play Store entry unaffected. -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <!-- TV has no touchscreen. Declaring it optional keeps the Play Store from
         filtering the app out on TV devices entirely. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- play-services-ads-identifier (pulled in transitively by firebase-analytics)
         declares AD_ID in its own manifest; that merges into ours and triggers
         Play's advertising-ID declaration requirement even though we never use it.
         Remove it explicitly so the merged APK manifest is clean. -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />

    <!-- Network for Open-Meteo + Gemini. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Cast SDK uses Wi-Fi state to gate discovery, and mDNS multicast to
         find smart displays on the LAN. Both are normal-level permissions —
         granted automatically, no runtime prompt. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Location at notify time (coarse is sufficient for weather).
         BACKGROUND is required so the WorkManager worker can read location when the
         app is not in the foreground. The user must grant it explicitly via the
         system Settings deep-link prompted from our LocationCard. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Daily notification. POST_NOTIFICATIONS is runtime on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- The scheduled briefing speaks from a WorkManager worker, i.e. the
         background. Android 15+ refuses audio focus to background apps and
         Android 17+ silences background AudioTrack writes outright, so the
         worker promotes itself to a media-playback foreground service for the
         brief delivery window (it can start one from the background because the
         run is triggered by an exact alarm — an explicit FGS-start exemption). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- ScheduledDeliveryService runs as a dataSync FGS during the fetch +
         insight-generation window (and during deliver when the period doesn't
         play speech). The mediaPlayback permission above covers the upgrade to
         the speech window. See docs/schedule-lifecycle.md. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Read today's calendar events to enrich the morning insight. Opt-in via the
         Settings toggle; runtime grant requested from there. We deliberately do NOT
         declare WRITE_CALENDAR — ClothesCast only reads. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- Re-arm the alarm on reboot, package update, timezone change, locale change. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Exact alarm at 7am.
         API 33+: USE_EXACT_ALARM is no-prompt and policy-friendly for alarm-clock-class apps.
         API 31-32: SCHEDULE_EXACT_ALARM is granted by default; we cap maxSdkVersion to avoid
         the API 33+ user prompt and Play Store declaration form. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <!-- Package visibility (API 30+ filtering applies on every supported
         device): the device-voice path constructs TextToSpeech engines and
         enumerates voices (AndroidTtsEngine / AndroidTtsVoiceEnumerator),
         which requires declaring the TTS_SERVICE intent or engine resolution
         and getEngines()/voice listing silently come up empty. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:name=".ClothesCastApplication"
        android:allowBackup="false"
        tools:ignore="UnusedAttribute"
        android:banner="@drawable/tv_banner"
        android:enableOnBackInvokedCallback="true"
        android:icon="${launcherIconRes}"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClothesCast">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ClothesCast">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- TV home-screen launcher. A separate intent-filter is required so
                 the phone and TV launchers both work from the same activity. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Bug-report screenshot sharing. The PNG is written to cacheDir/bug-reports/
             and exposed read-only to the app the user picks in the share sheet.
             Authority follows the ${applicationId}.fileprovider convention so the
             debug build's `.debug` suffix doesn't collide with the release build. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Internal — only the OS delivers via PendingIntent. -->
        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="app.clothescast.alarm.FIRE" />
                <action android:name="app.clothescast.alarm.FIRE_TONIGHT" />
            </intent-filter>
        </receiver>

        <!-- Owns the "Preparing your ClothesCast" / "Delivering your ClothesCast"
             foreground-service notification across an alarm-triggered delivery.
             Started from AlarmReceiver via startForegroundService inside the
             exact-alarm FGS-from-background exemption window. dataSync covers
             the fetch + insight phase (and the no-speech deliver phase);
             mediaPlayback covers the phone-speech deliver phase so the speaker
             can claim audio focus on Android 15+. See
             docs/schedule-lifecycle.md. -->
        <service
            android:name=".alarm.ScheduledDeliveryService"
            android:exported="false"
            android:foregroundServiceType="dataSync|mediaPlayback" />

        <!-- Internal — only the OS delivers via the notification delete
             PendingIntent. Logs user dismissals of the insight notifications so a
             forecast post that vanishes without a matching dismissal line points
             at a non-user removal (e.g. foreground-service teardown). -->
        <receiver
            android:name=".notification.NotificationDismissReceiver"
            android:exported="false" />

        <!-- System broadcasts that should re-arm the daily alarm. All five
             actions are protected system broadcasts, which the OS delivers
             to unexported receivers — exported="false" just keeps other
             apps from poking the receiver directly. TIME_SET matters because
             alarms are armed as absolute RTC epoch millis: correcting a wrong
             clock shifts the armed instant relative to the intended wall-clock
             time, so the fire time must be recomputed from the fixed clock. -->
        <receiver
            android:name=".alarm.ScheduleRefreshReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Opt out of Firebase Analytics advertising-ID (GAID) collection.
             The app's privacy policy explicitly states no advertising identifiers
             are collected; this flag enforces that at the SDK level on all API
             levels, not just Android 13+ where the missing AD_ID permission
             already blocks access. -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />

        <!-- Cast SDK loads CastOptionsProvider via reflection from this name.
             Receiver app id (Default Media Receiver) and discovery filters
             live in that class. -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="app.clothescast.cast.CastOptionsProvider" />

        <!-- Home-screen App Widget showing the current period's outfit.
             exported=true is required for the OS launcher to bind. -->
        <receiver
            android:name=".widget.OutfitWidgetReceiver"
            android:label="@string/widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/outfit_widget_info" />
        </receiver>

        <!-- Home-screen App Widget: the current period's feels-like chart.
             exported=true is required for the OS launcher to bind. -->
        <receiver
            android:name=".widget.FeelsLikeWidgetReceiver"
            android:label="@string/feels_like_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/feels_like_widget_info" />
        </receiver>

        <!-- Home-screen App Widget: the 7-day feels-like chart. -->
        <receiver
            android:name=".widget.SevenDayFeelsLikeWidgetReceiver"
            android:label="@string/feels_like_week_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/feels_like_week_widget_info" />
        </receiver>

        <!-- Home-screen App Widget: the at-a-glance conditions strip
             (thermometer + rain). exported=true is required for the OS
             launcher to bind. -->
        <receiver
            android:name=".widget.ConditionsWidgetReceiver"
            android:label="@string/conditions_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/conditions_widget_info" />
        </receiver>

        <!-- WorkManager hosts foreground workers in its own SystemForegroundService.
             API 34+ requires the service to declare every foregroundServiceType it
             runs with; merge the media-playback type onto WorkManager's declaration
             so FetchAndNotifyWorker.setForeground(...) can use it for spoken delivery. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="mediaPlayback"
            tools:node="merge" />

    </application>

</manifest>
