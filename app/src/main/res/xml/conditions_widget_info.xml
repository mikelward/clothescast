<?xml version="1.0" encoding="utf-8"?>
<!--
    AppWidget metadata for the at-a-glance conditions widget — a horizontal
    strip (thermometer + rain droplet), so it defaults wide and short (roughly
    3x1 cells on a 110dp grid) while staying resizable. updatePeriodMillis is 0
    because Glance refreshes are pushed by the fetch worker via
    updateAllClothesCastWidgets() — no polling.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="40dp"
    android:targetCellWidth="3"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:previewImage="@drawable/ic_widget_chart"
    android:description="@string/conditions_widget_description" />
