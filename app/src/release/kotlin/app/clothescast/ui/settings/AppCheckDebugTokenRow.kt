package app.clothescast.ui.settings

import androidx.compose.runtime.Composable

/**
 * Release-variant stub. Debug builds (locally — see
 * `ensureAppCheckDebugToken` in `app/build.gradle.kts`) render a
 * copyable App Check debug token row here; release builds use Play
 * Integrity and have no debug token to surface. Keeping this as a
 * no-op composable lets the call site stay variant-agnostic.
 */
@Composable
internal fun AppCheckDebugTokenRow() {
    // Intentionally empty.
}
