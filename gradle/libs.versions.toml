[versions]
# AGP 9.x pairs with the Gradle 9.x wrapper (kept in lockstep in
# gradle-wrapper.properties — currently 9.7.0; AGP 9.3 requires Gradle 9.5.0+
# and JDK 17+, we build on 21). AGP 9 ships Kotlin support built-in, so the
# standalone kotlin-android plugin is gone — Kotlin compiler options live in a
# top-level `kotlin { compilerOptions { } }` block in app/build.gradle.kts.
# Roborazzi must be >= 1.57 here: earlier releases use AGP's removed
# `TestedExtension` and fail to apply on AGP 9.
agp = "9.3.1"
kotlin = "2.4.10"
# Android API floor. Single source of truth: `app/build.gradle.kts` reads this
# for `minSdk` and re-emits it as `BuildConfig.MIN_SDK_VERSION` so the runtime
# can defend against APKs landing on devices below the floor. The OS package
# manager normally enforces this at install time, but Firebase has shown the
# app crashing on a OnePlus 8 Pro reporting Android 11 (API 30 — see PR #369)
# against this minSdk=31 APK. Whatever install path produced that, the
# resulting Crashlytics noise is from a configuration we never built for, so
# Telemetry silences Firebase when SDK_INT < MIN_SDK_VERSION.
minSdk = "31"
ksp = "2.3.11"
coreKtx = "1.19.0"
lifecycle = "2.11.0"
activityCompose = "1.13.0"
composeBom = "2026.08.00"
# Navigation Compose — type-safe routes (@Serializable destinations) require
# 2.8.0+. 2.9.x is the current stable line (2.10.0 is still alpha); it depends
# on lifecycle 2.9.x but composes fine against the 2.10.0 we pin above.
navigation = "2.9.8"
# Drag-and-drop reordering for Compose lists/columns (used by the Settings →
# Display "Home screen layout" reorder UI). 3.1.0 needs Compose foundation
# 1.7.0+, well under the BoM above. Lives on Maven Central.
reorderable = "3.1.0"
junit5 = "6.1.3"
mockk = "1.14.11"
kotest = "6.2.4"
turbine = "1.2.1"
coroutines = "1.11.0"
ktor = "3.5.2"
zxing = "3.5.4"
serialization = "1.11.0"
tink = "1.23.0"
datastore = "1.2.1"
work = "2.11.2"
# Glance powers the home-screen App Widget. Held at 1.1.1: 1.2.x exists only as
# a release candidate (no stable yet) and is what the live-widget-preview API
# needs — tracked in #914. compileSdk is no longer the blocker; the RC status is.
glance = "1.1.1"
# Vico charting (forecast graphs). Vico 3.x — the 3.0 "rewrite" was really a
# module consolidation (the Jetpack-only `compose` module dropped, the
# `multiplatform` module renamed to `compose` and promoted to stable, `core`
# folded in). The chart / axis / model-producer API we use is the same as 2.x
# apart from imports moving `com.patrykandpatrick.vico.core.*` ->
# `…vico.compose.*`, and the axis builders (rememberStart/rememberBottom)
# becoming Companion members so their old top-level imports drop. The one real
# code change was the custom scrub Decoration in ChartScrubber.kt: its
# CartesianDrawingContext.layerBounds is now a Compose Rect, not an android RectF.
vico = "3.3.0"
# Roborazzi renders @Preview composables to PNG via Robolectric — no emulator
# needed, runs in :app's JVM unit tests job. The CI step uploads the PNGs as a
# workflow artifact for human review on every PR (no diff gate, by intent).
roborazzi = "1.72.0"
robolectric = "4.16.1"
junit4 = "4.13.2"
# Play in-app updates: AppUpdateManager + the FLEXIBLE flow (background download
# + "Restart to install" prompt). Used to surface the update-available banner on
# the Today screen for Play-Store installs only.
playAppUpdate = "2.1.0"
# Firebase Analytics + Crashlytics. The BoM aligns SDK versions; the two Gradle
# plugins (gms-google-services, firebase-crashlytics) are applied conditionally
# in app/build.gradle.kts based on app/google-services.json's presence so CI
# (which doesn't carry the file) still compiles. See PRIVACY.md for the data
# boundary contract these SDKs operate under.
firebaseBom = "34.17.0"
gmsGoogleServices = "4.5.0"
firebaseCrashlyticsPlugin = "3.0.7"
# HiveMQ MQTT Client — used by the optional Smart Home bridge to publish the
# rendered insight prose to a user-hosted MQTT broker (Mosquitto add-on inside
# Home Assistant, typically). Pure-Java + Netty; no JNI. The publisher uses the
# blocking client wrapped in withContext(Dispatchers.IO), so no Reactor /
# CompletableFuture await plumbing is needed in :app.
hivemqMqttClient = "1.3.17"
# Google Cast — pulls in androidx.mediarouter for the route discovery /
# session APIs and `play-services-cast` for RemoteMediaClient. Cast SDK
# itself doesn't need google-services.json; framework init is gated at
# runtime on Google Play services availability, so CI builds without the
# JSON still compile and assemble.
playServicesCast = "22.3.1"
# Guava is not used directly — it arrives transitively via firebase-analytics
# (-> play-services-measurement, which still pins 31.1-android even in its
# latest release). We force it up to clear CVE-2023-2976 / GHSA-7g45-4rm6-3mm3
# (moderate: FileBackedOutputStream temp-dir disclosure, fixed in 32.0.0). See
# the dependency constraint in app/build.gradle.kts.
guava = "33.6.0-android"
# AboutLibraries — the open-source attribution list behind Settings → About →
# Licenses. The Gradle plugin walks the dependency graph and exports it as JSON;
# the `-compose-m3` artifact is only the runtime JSON reader (`rememberLibraries`),
# not its stock list UI, which we don't use. See app/build.gradle.kts for why the
# export is committed to res/raw instead of generated at build time.
aboutlibraries = "12.2.4"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-compose = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-compose", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-ktx = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-ktx", version.ref = "lifecycle" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigation" }
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-compose-material3 = { group = "androidx.compose.material3", name = "material3" }
androidx-compose-material-icons-core = { group = "androidx.compose.material", name = "material-icons-core" }
androidx-compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
reorderable = { group = "sh.calvin.reorderable", name = "reorderable", version.ref = "reorderable" }

kotlinx-coroutines-core = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-core", version.ref = "coroutines" }
kotlinx-coroutines-play-services = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-play-services", version.ref = "coroutines" }
kotlinx-coroutines-test = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-test", version.ref = "coroutines" }

junit-bom = { group = "org.junit", name = "junit-bom", version.ref = "junit5" }
junit-jupiter-api = { group = "org.junit.jupiter", name = "junit-jupiter-api", version.ref = "junit5" }
junit-jupiter-engine = { group = "org.junit.jupiter", name = "junit-jupiter-engine", version.ref = "junit5" }
junit-jupiter-params = { group = "org.junit.jupiter", name = "junit-jupiter-params", version.ref = "junit5" }
# Version comes from the junit-bom (declared on the test classpath), so no version.ref here.
junit-platform-launcher = { group = "org.junit.platform", name = "junit-platform-launcher" }
mockk = { group = "io.mockk", name = "mockk", version.ref = "mockk" }
kotest-assertions-core = { group = "io.kotest", name = "kotest-assertions-core", version.ref = "kotest" }
turbine = { group = "app.cash.turbine", name = "turbine", version.ref = "turbine" }

ktor-client-core = { group = "io.ktor", name = "ktor-client-core", version.ref = "ktor" }
ktor-client-cio = { group = "io.ktor", name = "ktor-client-cio", version.ref = "ktor" }
ktor-client-okhttp = { group = "io.ktor", name = "ktor-client-okhttp", version.ref = "ktor" }
ktor-client-content-negotiation = { group = "io.ktor", name = "ktor-client-content-negotiation", version.ref = "ktor" }
ktor-client-logging = { group = "io.ktor", name = "ktor-client-logging", version.ref = "ktor" }
ktor-client-mock = { group = "io.ktor", name = "ktor-client-mock", version.ref = "ktor" }
ktor-serialization-kotlinx-json = { group = "io.ktor", name = "ktor-serialization-kotlinx-json", version.ref = "ktor" }
ktor-server-cio = { group = "io.ktor", name = "ktor-server-cio", version.ref = "ktor" }
ktor-server-partial-content = { group = "io.ktor", name = "ktor-server-partial-content", version.ref = "ktor" }
ktor-server-auto-head-response = { group = "io.ktor", name = "ktor-server-auto-head-response", version.ref = "ktor" }
zxing-core = { group = "com.google.zxing", name = "core", version.ref = "zxing" }
kotlinx-serialization-json = { group = "org.jetbrains.kotlinx", name = "kotlinx-serialization-json", version.ref = "serialization" }

tink-android = { group = "com.google.crypto.tink", name = "tink-android", version.ref = "tink" }

androidx-datastore-preferences = { group = "androidx.datastore", name = "datastore-preferences", version.ref = "datastore" }
androidx-datastore-preferences-core = { group = "androidx.datastore", name = "datastore-preferences-core", version.ref = "datastore" }

androidx-work-runtime-ktx = { group = "androidx.work", name = "work-runtime-ktx", version.ref = "work" }
androidx-work-testing = { group = "androidx.work", name = "work-testing", version.ref = "work" }

androidx-glance-appwidget = { group = "androidx.glance", name = "glance-appwidget", version.ref = "glance" }
androidx-glance-material3 = { group = "androidx.glance", name = "glance-material3", version.ref = "glance" }

vico-compose-m3 = { group = "com.patrykandpatrick.vico", name = "compose-m3", version.ref = "vico" }

# Snapshot testing — see roborazzi version comment above for rationale.
roborazzi = { group = "io.github.takahirom.roborazzi", name = "roborazzi", version.ref = "roborazzi" }
roborazzi-compose = { group = "io.github.takahirom.roborazzi", name = "roborazzi-compose", version.ref = "roborazzi" }
robolectric = { group = "org.robolectric", name = "robolectric", version.ref = "robolectric" }
junit4 = { group = "junit", name = "junit", version.ref = "junit4" }
# JUnit Vintage lets the JUnit 5 platform run JUnit 4 tests. Roborazzi/Robolectric
# require @RunWith(AndroidJUnit4::class) (a JUnit 4 mechanism), so we keep the
# JUnit 5 platform and let Vintage run the snapshot tests alongside.
junit-vintage-engine = { group = "org.junit.vintage", name = "junit-vintage-engine", version.ref = "junit5" }
androidx-compose-ui-test-junit4 = { group = "androidx.compose.ui", name = "ui-test-junit4" }
androidx-compose-ui-test-manifest = { group = "androidx.compose.ui", name = "ui-test-manifest" }

play-app-update-ktx = { group = "com.google.android.play", name = "app-update-ktx", version.ref = "playAppUpdate" }

firebase-bom = { group = "com.google.firebase", name = "firebase-bom", version.ref = "firebaseBom" }
firebase-analytics = { group = "com.google.firebase", name = "firebase-analytics" }
firebase-crashlytics = { group = "com.google.firebase", name = "firebase-crashlytics" }
firebase-appcheck-playintegrity = { group = "com.google.firebase", name = "firebase-appcheck-playintegrity" }
firebase-appcheck-debug = { group = "com.google.firebase", name = "firebase-appcheck-debug" }
firebase-auth = { group = "com.google.firebase", name = "firebase-auth" }
firebase-installations = { group = "com.google.firebase", name = "firebase-installations" }

hivemq-mqtt-client = { group = "com.hivemq", name = "hivemq-mqtt-client", version.ref = "hivemqMqttClient" }

play-services-cast-framework = { group = "com.google.android.gms", name = "play-services-cast-framework", version.ref = "playServicesCast" }
guava = { group = "com.google.guava", name = "guava", version.ref = "guava" }
aboutlibraries-compose-m3 = { group = "com.mikepenz", name = "aboutlibraries-compose-m3", version.ref = "aboutlibraries" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
android-library = { id = "com.android.library", version.ref = "agp" }
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
roborazzi = { id = "io.github.takahirom.roborazzi", version.ref = "roborazzi" }
gms-google-services = { id = "com.google.gms.google-services", version.ref = "gmsGoogleServices" }
firebase-crashlytics = { id = "com.google.firebase.crashlytics", version.ref = "firebaseCrashlyticsPlugin" }
aboutlibraries = { id = "com.mikepenz.aboutlibraries.plugin", version.ref = "aboutlibraries" }
